### Theme
	# Font
	font = /usr/share/fonts/NerdFonts/ttf/CaskaydiaCoveNerdFont-Regular.ttf
	font-size = 18
	hint-font = false

	text-color = #F1D1C1

	# Prompt text theme
	prompt-color = #FFBB22
	prompt-background = #2A2A2AAA
	prompt-background-padding = 0,733,1,5
	prompt-background-corner-radius = 8

	# Placeholder text theme
	placeholder-color = #8B3513
	placeholder-background = #00FF0000
	placeholder-background-padding = 0
	placeholder-background-corner-radius = 0

	# Input text theme
	# input-color = #FFFFFF
	input-background = #00000000
	input-background-padding = 0
	input-background-corner-radius = 0

	# Default result text theme
	# default-result-color = #FFFFFF
	default-result-background = #00000000
	default-result-background-padding = 0
	default-result-background-corner-radius = 0

	# Alternate (even-numbered) result text theme
	# alternate-result-color = #FFFFFF
	# alternate-result-background = #00000000
	# alternate-result-background-padding = 0
	# alternate-result-background-corner-radius = 0

	# Selection text
	selection-color = #FF4455
	selection-background = #00000000
	selection-background-padding = 0
	selection-background-corner-radius = 0

	# Matching portion of selection text
	selection-match-color = #FF7700


	# Cursor
	text-cursor-style = block
	text-cursor-corner-radius = 0

### Window theme
	# Window background color
	background-color = #1F1C1CEA

	# Width of the border outlines in pixels.
	outline-width = 1

	# Border outline color
	outline-color = #000000

	# Width of the border in pixels.
	border-width = 2

	# Border color
	border-color = #F1D1C1

	# Radius of window corners in pixels.
	corner-radius = 12

### Text layout
	# Prompt to display.
	prompt-text = " "

	# Extra horizontal padding between prompt and input.
	prompt-padding = 16

	# Placeholder input text.
	placeholder-text = "Cerca"

	# Maximum number of results to display.
	# If 0, tofi will draw as many results as it can fit in the window.
	num-results = 0

	# Spacing between results and input in pixels. Can be negative.
	result-vert-spacing = 8

	# Spacing between results in pixels. Can be negative.
	result-spacing = 8

	# List results horizontally.
	horizontal = false

	# Minimum width of input in horizontal mode.
	min-input-width = 0

### Window
	# Width and height of the window. Can be pixels or a percentage.
	width = 810
	height = 420

	# Padding between borders and text. Can be pixels or a percentage.
	padding-top = 16
	padding-bottom = 8
	padding-left = 8
	padding-right = 8

	clip-to-padding = true
	scale = true

### Window positioning
	anchor = center

	exclusive-zone = -1

	margin-top = 5
	margin-bottom = 0
	margin-left = 0
	margin-right = 0

### Behaviour
	# Hide the mouse cursor.
	hide-cursor = false

	# Show a text cursor in the input field.
	text-cursor = false

	# Sort results by number of usages in run and drun modes.
	history = true

	matching-algorithm = fuzzy

	require-match = true

	auto-accept-single = false
